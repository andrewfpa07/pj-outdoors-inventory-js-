<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Product - PJ Outdoors Inventory</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        html, body { height: 100%; margin: 0; }
        body { background-color: #2f2f2f; color: white; font-family: Arial, sans-serif; }
        .page-container { display: flex; flex-direction: column; min-height: 100vh; }
        .content-wrap { flex: 1; padding: 20px; }
        .navbar { background-color: #444; padding: 10px 20px; display: flex; gap: 15px; flex-wrap: wrap; }
        .navbar a { color: white; text-decoration: none; font-weight: bold; }
        .navbar a:hover { color: #FFA500; }
        .container-card {
            background-color: #3f3f3f;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 15px rgba(0,0,0,0.3);
            max-width: 600px;
            margin: auto;
        }
        label { font-weight: bold; color: white; }
        input, select {
            width: 100%;
            padding: 8px;
            margin-top: 5px;
            margin-bottom: 15px;
            border-radius: 5px;
            border: 1px solid #555;
            background-color: #555;
            color: white;
        }
        input:focus, select:focus { outline: none; border-color: #FFA500; box-shadow: 0 0 5px #FFA500; }
        .btn-update {
            background-color: #FFA500;
            border: none;
            color: #000;
            width: 100%;
            padding: 10px;
            font-weight: bold;
            transition: 0.3s;
        }
        .btn-update:hover { background-color: #e69500; }
        .btn-back {
            background-color: #555;
            border: none;
            color: white;
            width: 100%;
            padding: 10px;
            margin-top: 10px;
            border-radius: 5px;
            transition: 0.3s;
        }
        .btn-back:hover { background-color: #444; }
        footer { text-align: center; padding: 10px; color: #FFA500; font-weight: bold; background-color: #2f2f2f; }
        @media (max-width: 768px) {
            .navbar { flex-direction: column; gap: 10px; }
            .container-card { padding: 15px; }
        }
    </style>
</head>
<body>
<div class="page-container">
    <div class="navbar">
        <a href="/">Home</a>
        <a href="/add">Add Product</a>
        <a href="/search">Search</a>
        <a href="/index">All Products</a>
    </div>

    <div class="content-wrap container">
        <div class="container-card">
            <h2 class="mb-3">Update Product</h2>
            <form action="/update/<%= product_id %>" method="POST">
                <label for="name">Product Name:</label>
                <input type="text" id="name" name="name" value="<%= product.name %>" required>

                <label for="container">Container Number:</label>
                <select id="container" name="container" required>
                    <% containers.forEach(i => { %>
                    <option value="<%= i %>" <%= product.container == i ? 'selected' : '' %>>Container <%= i %></option>
                    <% }); %>
                </select>

                <label for="side">Side:</label>
                <select id="side" name="side" required>
                    <% sides.forEach(letter => { %>
                    <option value="<%= letter %>" <%= product.side == letter ? 'selected' : '' %>><%= letter %></option>
                    <% }); %>
                </select>

                <label for="shelf">Shelf Number:</label>
                <input type="number" id="shelf" name="shelf" value="<%= product.shelf %>" min="1" required>

                <label for="quantity">Quantity:</label>
                <input type="number" id="quantity" name="quantity" value="<%= product.quantity %>" min="0">

                <button type="submit" class="btn-update">Update Product</button>
            </form>

            <a class="btn-back mt-3" href="/index">Back to All Products</a>
        </div>
    </div>

    <footer>
        &copy; 2025 PJ Outdoors Inventory System
    </footer>
</div>
</body>
</html>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Products - PJ Outdoors Inventory</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        html, body { height: 100%; margin: 0; }
        body { background-color: #2f2f2f; color: white; font-family: Arial, sans-serif; }
        .page-container { display: flex; flex-direction: column; min-height: 100vh; }
        .content-wrap { flex: 1; padding: 20px; }
        .navbar {
            background-color: #444;
            padding: 10px 20px;
            display: flex;
            gap: 15px;
            flex-wrap: wrap;
        }
        .navbar a { color: white; text-decoration: none; font-weight: bold; }
        .navbar a:hover { color: #FFA500; }
        .container-card {
            background-color: #3f3f3f;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 15px rgba(0,0,0,0.3);
            max-width: 1000px;
            margin: auto;
        }
        table.table {
            width: 100%;
            border-collapse: separate;
            border-spacing: 0;
            border-radius: 8px;
            overflow: hidden;
        }
        table.table th { background-color: #555; color: white; text-align: left; padding: 10px; }
        table.table td { background-color: #fff; color: #000; padding: 10px; }
        .low-stock td { background-color: #ff6666 !important; color: #000 !important; }
        .btn-warning { background-color: #FFA500; border: none; color: #000; transition: 0.3s; }
        .btn-warning:hover { background-color: #e69500; }
        .btn-danger { background-color: #ff4d4d; border: none; transition: 0.3s; }
        .btn-danger:hover { background-color: #e60000; }
        .btn-secondary { background-color: #555; color: white; border-radius: 5px; }
        footer { text-align: center; padding: 10px; color: #FFA500; font-weight: bold; background-color: #2f2f2f; }
        @media (max-width: 768px) {
            .navbar { flex-direction: column; gap: 10px; }
            .container-card { padding: 15px; }
        }
    </style>
</head>
<body>
<div class="page-container">
    <div class="navbar">
        <a href="/">Home</a>
        <a href="/add">Add Product</a>
        <a href="/search">Search</a>
        <a href="/index">All Products</a>
    </div>

    <div class="content-wrap container">
        <div class="container-card">
            <h2 class="mb-3">All Products</h2>
            <% if (products.length > 0) { %>
            <table class="table">
                <thead>
                    <tr>
                        <th>Name</th>
                        <th>Container</th>
                        <th>Side</th>
                        <th>Shelf</th>
                        <th>Quantity</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <% products.forEach(p => { %>
                    <tr class="<%= p.quantity <= 5 ? 'low-stock' : '' %>">
                        <td><%= p.name %></td>
                        <td><%= p.container %></td>
                        <td><%= p.side %></td>
                        <td><%= p.shelf %></td>
                        <td><%= p.quantity %></td>
                        <td>
                            <a class="btn btn-sm btn-warning" href="/update/<%= p.id %>">Update</a>
                            <form action="/delete/<%= p.id %>" method="POST" style="display:inline;">
                                <button class="btn btn-sm btn-danger" type="submit" onclick="return confirm('Delete this product?')">Delete</button>
                            </form>
                        </td>
                    </tr>
                    <% }); %>
                </tbody>
            </table>
            <% } else { %>
            <p>No products in inventory.</p>
            <% } %>
            <a class="btn btn-secondary mt-3 w-100" href="/">Back to Home</a>
        </div>
    </div>

    <footer>
        &copy; 2025 PJ Outdoors Inventory System
    </footer>
</div>
</body>
</html>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search Product - PJ Outdoors Inventory</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background-color: #2f2f2f; color: white; font-family: Arial, sans-serif; }
        .navbar {
            background-color: #444;
            padding: 10px 20px;
            display: flex;
            gap: 15px;
            flex-wrap: wrap;
        }
        .navbar a { color: white; text-decoration: none; font-weight: bold; }
        .navbar a:hover { color: #FFA500; }
        .container-card {
            background-color: #3f3f3f;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 15px rgba(0,0,0,0.3);
        }
        label { font-weight: bold; color: white; }
        input {
            width: 100%;
            padding: 8px;
            margin-top: 5px;
            margin-bottom: 15px;
            border-radius: 5px;
            border: 1px solid #555;
            background-color: #555;
            color: white;
        }
        input:focus { outline: none; border-color: #FFA500; box-shadow: 0 0 5px #FFA500; }
        .btn-search {
            background-color: #FFA500;
            border: none;
            color: #000;
            width: 100%;
            padding: 10px;
            font-weight: bold;
        }
        .btn-search:hover { background-color: #e69500; }
        .btn-back {
            background-color: #555;
            border: none;
            color: white;
            width: 100%;
            padding: 10px;
            margin-top: 10px;
        }
        table { width: 100%; margin-top: 15px; border-collapse: collapse; }
        th, td { padding: 10px; text-align: left; }
        th { background-color: #444; }
        tr:nth-child(even) { background-color: #555; }
        tr.low-stock { background-color: #FFB347 !important; color: #000; }
        footer { text-align: center; padding: 10px; margin-top: 40px; color: #FFA500; font-weight: bold; }
        @media (max-width: 768px) {
            .navbar { flex-direction: column; gap: 10px; }
            .container-card { padding: 15px; }
        }
    </style>
</head>
<body>

<div class="navbar">
    <a href="/">Home</a>
    <a href="/add">Add Product</a>
    <a href="/search">Search</a>
    <a href="/index">All Products</a>
</div>

<div class="container mt-5">
    <div class="container-card">
        <h2 class="mb-3">Search Products</h2>
        <form action="/search" method="POST">
            <label for="name">Product Name:</label>
            <input type="text" id="name" name="name" placeholder="Enter product name..." required>
            <button type="submit" class="btn-search">Search</button>
        </form>

        <% if (results && results.length > 0) { %>
        <table class="table table-striped mt-3">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Container</th>
                    <th>Side</th>
                    <th>Shelf</th>
                    <th>Quantity</th>
                </tr>
            </thead>
            <tbody>
                <% results.forEach(p => { %>
                <tr class="<%= p.quantity <= 5 ? 'low-stock' : '' %>">
                    <td><%= p.name %></td>
                    <td><%= p.container %></td>
                    <td><%= p.side %></td>
                    <td><%= p.shelf %></td>
                    <td><%= p.quantity %></td>
                </tr>
                <% }); %>
            </tbody>
        </table>
        <% } else if (results !== null) { %>
        <p>No matching products found.</p>
        <% } %>

        <a class="btn-back mt-3" href="/">Back to Home</a>
    </div>
</div>

<footer>
    &copy; 2025 PJ Outdoors Inventory System
</footer>

</body>
</html>